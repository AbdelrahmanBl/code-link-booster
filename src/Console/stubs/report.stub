<?php

namespace {{ namespace }};

use CodeLink\Booster\Facades\Booster;

class {{ class }}
{
    public static function get()
    {
        return Booster::report()
        ->addStatistic('title', 'value')
        ->addPieChart(Booster::chart())
        ->addBarChart(Booster::chart())
        ->addLineChart(Booster::chart())
        ->generate();
    }
}
